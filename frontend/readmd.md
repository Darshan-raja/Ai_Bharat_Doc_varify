How to run the project

cd Frontend
npm install
npm run build
npm start

cd Backend
npm install
npm run start

# To run the project, you need to have Node.js and npm installed on your computer. Once you have Node.js and npm installed, you can follow these steps to run the project:

# 1. Clone the repository to your local machine.